from __future__ import annotations

# Mark the app directory as a package so relative imports
# like `from .core import ...` work correctly when running under
# FastAPI/uvicorn and inside Docker images.
