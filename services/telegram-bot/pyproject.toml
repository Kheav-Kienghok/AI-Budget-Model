[project]
name = "telegram-bot"
version = "0.1.0"
description = "Telegram chatbot that uses the model API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot>=21.5",
    "httpx>=0.28.1",
    "python-dotenv>=1.0.1",
]
